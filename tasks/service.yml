---
- name: Start/enable service
  service:
    name: docker
    state: started
    enabled: yes
  become: true

- name: Start/enable docker socket service
  service:
    name: docker.socket
    state: started
    enabled: yes
  when: ansible_os_family == 'Debian'
  become: true
